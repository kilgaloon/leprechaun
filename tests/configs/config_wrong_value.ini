# we want to log our errors, and specify where
client.error_log = 334
# we want to log our info about app to know whats going on
client.info_log = 434.23
# path to recipes directory
client.recipes_path = true

client.pid_file = false
# lock file is only present when client is busy
client.lock_file = -1

# we want to log our errors, and specify where
server.error_log = 20.18.2001
# we want to log our info about app to know whats going on
server.info_log = 00/00/00
# path to recipes directory
server.recipes_path = disaster

server.port = cyclop

server.pid_file = ...
# lock file is only present when client is busy
server.lock_file = rumba cha cha cha