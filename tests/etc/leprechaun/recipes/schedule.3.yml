name: schedule3 # name of recipe
definition: schedule
schedule: 
    min: 0
    hour: 0
    day: 0

steps: # steps are done from first to last
    - -> echo "this is async task"
    - echo "not gonna wait for ping"