# we want to log our errors, and specify where
error_log = /var/log/rainbow/error-client.log
# path to recipes directory
recipes_path = ./recipes

[variables]
# I need this for rainbow server
host = [::]
# and this also
port = 11200